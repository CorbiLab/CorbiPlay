import { ComingSoon } from "@/components/shared/coming-soon";

export default function GpsProvidersSettingsPage() {
  return (
    <ComingSoon
      title="GPS Providers"
      note="Provider connections are implemented behind an adapter interface once real credentials exist — see docs/INTEGRATIONS.md. Never faked."
    />
  );
}
